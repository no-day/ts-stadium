{
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @no-day/ts-stadium build; yarn workspace @no-day/ts-stadium-demo build",
    "test": "yarn workspace @no-day/ts-stadium test",
    "check": "yarn build; yarn test",
    "public": "ts-node scripts/public.ts",
    "release": "bash scripts/release.sh"
  },
  "dependencies": {
    "fp-ts": "^2.9.5"
  },
  "devDependencies": {
    "docs-ts": "^0.6.10",
    "lerna": "^4.0.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "name": "ts-stadium"
}
