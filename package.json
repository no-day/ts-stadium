{
  "private": "true",
  "workspaces": [
    "packages/ts-stadium",
    "packages/ts-stadium-demo"
  ],
  "scripts": {
    "build": "yarn workspace @no-day/ts-stadium build; yarn workspace @no-day/ts-stadium-demo build",
    "test": "yarn workspace @no-day/ts-stadium test",
    "docs": "yarn workspace @no-day/ts-stadium docs",
    "demo": "yarn workspace @no-day/ts-stadium-demo build --prefix-paths ts-stadium; cp -r packages/ts-stadium-demo/public docs/demo"
  },
  "dependencies": {
    "fp-ts": "^2.9.5"
  }
}
